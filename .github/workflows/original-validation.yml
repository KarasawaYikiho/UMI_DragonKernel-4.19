name: Original validation build

on:
  push:
    paths:
      - ".github/workflows/original-validation.yml"
      - "arch/**"
      - "block/**"
      - "certs/**"
      - "crypto/**"
      - "drivers/**"
      - "fs/**"
      - "include/**"
      - "init/**"
      - "ipc/**"
      - "kernel/**"
      - "lib/**"
      - "mm/**"
      - "net/**"
      - "security/**"
      - "sound/**"
      - "virt/**"
      - "scripts/dragonkernel/**"
      - "Documentation/dragonkernel/baseline.json"
      - "Kconfig"
      - "Makefile"
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  build:
    name: original-${{ matrix.device }}
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        device: [umi, cmi, cas, thyme, apollo]
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Set output directory
        run: echo "OUT_ROOT=$RUNNER_TEMP/dragonkernel-out" >> "$GITHUB_ENV"

      - name: Install build dependencies
        run: bash scripts/dragonkernel/bootstrap_wsl.sh

      - name: Fetch locked toolchain
        shell: bash
        run: scripts/dragonkernel/prepare_toolchain.sh "$RUNNER_TEMP" >> "$GITHUB_ENV"

      - name: Build original kernel
        run: scripts/dragonkernel/build_original.sh "${{ matrix.device }}"

      - name: Upload validation evidence
        uses: actions/upload-artifact@v7
        with:
          name: original-${{ matrix.device }}-${{ github.sha }}
          path: |
            ${{ env.OUT_ROOT }}/original/${{ matrix.device }}/.config
            ${{ env.OUT_ROOT }}/original/${{ matrix.device }}/include/config/kernel.release
            ${{ env.OUT_ROOT }}/original/${{ matrix.device }}/SHA256SUMS
            ${{ env.OUT_ROOT }}/original/${{ matrix.device }}/build.log
            ${{ env.OUT_ROOT }}/original/${{ matrix.device }}/arch/arm64/boot/Image
            ${{ env.OUT_ROOT }}/original/${{ matrix.device }}/arch/arm64/boot/dts/**/*.dtb
            ${{ env.OUT_ROOT }}/original/${{ matrix.device }}/arch/arm64/boot/dts/**/*.dtbo
            ${{ env.OUT_ROOT }}/original/${{ matrix.device }}/**/*.ko
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 14
          compression-level: 0
