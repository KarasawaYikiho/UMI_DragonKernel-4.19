# 游戏控制

控制器输入为运行时帧目标、CPU/GPU/DDR 负载和 thermal headroom；输出仅为归一化请求。它不包含游戏包名、固定刷新率、频率或 pwrlevel。

优先级：热安全 > 帧恢复 > 加载加速 > 日常预算。连续样本与滞回限制振荡；热压力立即清除帧恢复，恢复时逐步释放。UFS、CPU、GPU 和 DDR 请求必须短时、有 owner、可回读、可撤销。

当前无硬件写后端；待实机基线和桌面/超级岛对照完成后再决定是否启用。
